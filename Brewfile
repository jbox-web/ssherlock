# frozen_string_literal: true

# the harvest-licenses script relies on it (process substitution)
brew 'bash'

# to install other tools (pins Crystal, drives the dev:* tasks)
brew 'mise'

# link-time C dependency of ssh2.cr / libssh2 (the SSH transport)
brew 'libssh2'

# Crystal's garbage collector on macOS
brew 'bdw-gc'
